# 🔄 Миграция на новую структуру

## Что изменилось

### 📁 Новая структура проекта
- **Модульная архитектура**: Код разделен на логические модули
- **Чистая структура**: Удалены дублирующиеся файлы
- **Docker поддержка**: Добавлены Dockerfile и docker-compose.yml
- **Улучшенная конфигурация**: Использование pydantic-settings

### 🔧 Основные изменения

#### Файлы перемещены:
- `bot.py` → `app/bot/bot.py`
- `admin_app.py` → `app/admin/app.py`
- `models.py` → `app/models/models.py`
- `schemas.py` → `app/schemas/schemas.py`
- `auth.py` → `app/core/auth.py`
- `config.py` → `app/core/config.py`
- `run.py` → `main.py`

#### Новые файлы:
- `Dockerfile` - для контейнеризации
- `docker-compose.yml` - для удобного запуска
- `scripts/init_db.py` - инициализация БД
- `env.example` - пример конфигурации

## 🚀 Быстрая миграция

### 1. Создайте новый .env файл
```bash
cp env.example .env
# Отредактируйте .env с вашими настройками
```

### 2. Установите новые зависимости
```bash
pip install -r requirements.txt
```

### 3. Инициализируйте базу данных
```bash
python scripts/init_db.py
```

### 4. Запустите систему
```bash
python main.py
```

## 🐳 Docker (новое)

### Быстрый запуск
```bash
docker-compose up -d
```

### Ручная сборка
```bash
docker build -t telegram-crm .
docker run -d -p 8001:8001 --env-file .env telegram-crm
```

## 🔧 Исправленные проблемы

### ✅ Ошибки с сессиями БД
- Исправлены `DetachedInstanceError` в боте
- Правильное управление сессиями SQLAlchemy

### ✅ Проблемы с шаблонами
- Исправлены ошибки в Jinja2 шаблонах
- Убраны неработающие `url_for` и `request.args`

### ✅ Конфигурация
- Использование pydantic-settings для валидации
- Автоматическая загрузка из .env

## 📋 Чек-лист миграции

- [ ] Скопировать `env.example` в `.env`
- [ ] Обновить токен бота в `.env`
- [ ] Установить новые зависимости
- [ ] Инициализировать базу данных
- [ ] Протестировать запуск
- [ ] Проверить работу бота
- [ ] Проверить админку

## 🆘 Если что-то не работает

### Проблемы с импортами
```bash
# Убедитесь, что вы в корневой папке проекта
cd /path/to/telegram-crm
python main.py
```

### Проблемы с базой данных
```bash
# Удалите старую базу и пересоздайте
rm bot_database.db
python scripts/init_db.py
```

### Проблемы с зависимостями
```bash
# Обновите pip и переустановите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

## 🎉 Готово!

После миграции у вас будет:
- ✅ Чистая модульная архитектура
- ✅ Docker поддержка
- ✅ Исправленные ошибки
- ✅ Улучшенная документация
- ✅ Современные практики разработки
